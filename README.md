This template is based on Python 3.10

`pip install -r requirements.txt`

`pre-commit install`

The bot has Jishaku installed, see the following [command reference](https://github.com/Gorialis/jishaku).
